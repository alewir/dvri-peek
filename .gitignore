# Local config with real credentials / IPs — never commit
cameras.yaml

# go2rtc binary + generated config
go2rtc
go2rtc_*
go2rtc.generated.yaml

# Python
__pycache__/
*.pyc
.venv/
venv/

# Runtime / scratch
data/
*.log

# MCP config (contains auth token) — keep local
.mcp.json

# Local Pi operational doc (host-specific + credential refs)
.meta/raspi-setup.md

# Local plugin secrets (calendar ICS URL, etc.) — never commit
secrets.local.yaml
*.local.yaml
*.local.json
